import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  button: {
    padding: 10,
    borderRadius: 3,
    minWidth: 150,
    display: "flex",
    alignItems: "center",
    backgroundColor: "#2196f3"
  },
  buttonTitle: {
    color: "white"
  }
})